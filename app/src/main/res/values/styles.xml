<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="MyButton" parent="Base.Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/red</item>
        <item name="colorButtonNormal">@color/indigo</item>
    </style>
    <style name="Onwork" parent="Theme.AppCompat.Light">
    <item name="colorControlHighlight">#38ADAE</item>
    <item name="colorButtonNormal">@color/green</item>
    </style>
    <style name="blueBtn" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/blue</item>
        <item name="colorButtonNormal">@color/lightBlue</item>
    </style>
    <style name="DefaultNumberPickerTheme">
        <item name="android:textSize">18sp</item>
        <item name="colorControlNormal">@color/transparent</item>
    </style>


</resources>
